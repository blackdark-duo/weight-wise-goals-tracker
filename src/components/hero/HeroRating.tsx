
import React from "react";
import { motion } from "framer-motion";

const HeroRating = () => {
  // Component left in place but made empty to maintain file structure
  return null;
};

export default HeroRating;
